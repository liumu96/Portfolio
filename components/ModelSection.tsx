import React from "react";
import SectionHeading from "./SectionHeading";

const models = [
  {
    name: "Physics",
    categories: ["Blender", "Animation", "Physics"],
  },
];

const ModelSection = () => {
  return (
    <section id="models">
      <SectionHeading>Models</SectionHeading>
      {/* read more */}
    </section>
  );
};

export default ModelSection;
